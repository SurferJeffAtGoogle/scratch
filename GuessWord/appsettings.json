{
  "Logging": {
    "LogLevel": {
      "Default": "Warning"
    }
  },
  "AllowedHosts": "*",
  "SecretWord": {
    "SecretWord": "tabard"
  },

  "DataProtectionOptions": {
    // Specify your key name here, in resource name format:
    // "projects/[...]/locations/[...]/keyRings/[...]/cryptoKeys/[...]"
    "KmsKeyName": "projects/jonskeet-integration-tests/locations/global/keyRings/global-keyring/cryptoKeys/test_key_1",

    // Specify the bucket name and object name to use to store encrypted keys.
    // The object doesn't not need to exist beforehand, but the bucket does.
    "Bucket": "jonskeet-testbucket",
    "Object": "test-keys2.xml"
  }
}
