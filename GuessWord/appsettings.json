{
  "Logging": {
    "LogLevel": {
      "Default": "Warning"
    }
  },
  "AllowedHosts": "*",
  "SecretWord": {
    "SecretWord": "tabard"
  },

  "GoogleCloudKmsOptions": {
    // Specify your key name here, in resource name format:
    // "projects/[...]/locations/[...]/keyRings/[...]/cryptoKeys/[...]"
    "KeyName": "projects/jonskeet-integration-tests/locations/global/keyRings/global-keyring/cryptoKeys/test_key_1"
  },

  "GoogleCloudStorageOptions": {
    // Specify the bucket name and object name to use to store encrypted keys.
    // The object doesn't not need to exist beforehand, but the bucket does.
    "Bucket": "...",
    "Object": "..."
  }
}
