{
  "Logging": {
    "LogLevel": {
      "Default": "Warning"
    }
  },
  "AllowedHosts": "*",
  "SecretWord": {
    "SecretWord": "tabard"
  },

  "DataProtectionOptions": {
    // Specify your key name here, in resource name format:
    "KmsKeyName": "projects/[project_id]/locations/[location_id]/keyRings/[key_ring_id]/cryptoKeys/[key_id]",

    // Specify the bucket name and object name to use to store encrypted keys.
    // The object doesn't not need to exist beforehand, but the bucket does.
    "Bucket": "[bucket-name]",
    "Object": "[object-name]"
  }
}
